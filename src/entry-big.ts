// xtandard/big — exact-decimal intervals backed by big.js (peer `big.js`).
// A smaller-footprint sibling of xtandard/decimal. See docs/INTERVAL.md#decimal-intervals.

export { BigInterval, bigDomain } from "./interval-big.ts";
