// @xtandard/lib/ip — IPv4/IPv6 address intervals: the Interval<T> engine over IP
// addresses (peer `ipaddr.js`). Discrete; CIDR-aware. See docs/INTERVAL.md#ip-intervals.

export { Ipv4Interval, Ipv6Interval } from "./interval-ip.ts";
