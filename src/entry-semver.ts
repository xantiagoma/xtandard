// @xtandard/lib/semver — semantic-version intervals: the Interval<T> engine bound to
// semver version strings (peer `semver`). A version range IS an interval.
// See docs/INTERVAL.md#semver-intervals.

export { SemverInterval, semverDomain } from "./interval-semver.ts";
