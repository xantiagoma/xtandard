// xtandard/bignumber — exact-decimal intervals backed by bignumber.js
// (peer `bignumber.js`). A sibling of xtandard/decimal. See docs/INTERVAL.md#decimal-intervals.

export { BigNumberInterval, bigNumberDomain } from "./interval-bignumber.ts";
