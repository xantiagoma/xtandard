// xtandard/fraction — exact-RATIONAL intervals backed by fraction.js (peer
// `fraction.js`). 1/3 is lossless and renders as "1/3". See docs/INTERVAL.md#fraction-intervals.

export { FractionInterval, fractionDomain } from "./interval-fraction.ts";
